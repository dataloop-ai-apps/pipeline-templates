# RAG Preprocessing Template (Scaled)

This is a scaled version of the RAG Preprocessing Template with all related services configured to use `minReplicas: 1`, `maxReplicas: 1`, and `preemptible: false`.

For full documentation, see the [RAG Preprocessing Template](../preprocessing_for_rag/).
