# RAG Template (Scaled)

This is a scaled version of the RAG Template with all related services configured to use `minReplicas: 1`, `maxReplicas: 1`, and `preemptible: false`.

For full documentation, see the [RAG Template](../rag_template/).
